<tr>
  <td>
  <% if collection.photo.exists? %><%= link_to image_tag(collection.photo.url(:icon), :retina => true, :class => "curvedicon"), collection %><% else %><%= link_to image_tag("/system/stock/barbarian.gif", :retina => true, :class => "curvedicon"), collection %><% end %></td>
  <td><%= link_to collection.miniature.name, collection %></td>
   <td><%= collection.name %></td>
   <td><% collection.miniature.manufacturers.each do |manufacturer| %>
        <%= link_to manufacturer.name, manufacturer_path(manufacturer) %>
          <% end %></td>
  <td><%= collection.progress %></td>
  
  </td></tr>