    Replace photo
    <%= form_for @collection, :html => { :multipart => true } do |f| %>
     	<%= f.file_field :photo %>
     	<%= f.submit "Replace Photo", class: "btn btn-large btn-primary" %>
     <% end %>