<%= form_for(@sculptor) do |f| %>
  <% if @sculptor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sculptor.errors.count, "error") %> prohibited this sculptor from being saved:</h2>

      <ul>
      <% @sculptor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    <%= f.label :biog %><br />
    <%= f.text_area :biog %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
