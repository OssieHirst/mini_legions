<% @miniature %>

    <strong id="got" class="stat">
      <%= @miniature.collections.where(status: 'Got').count(:user_id, distinct: true) %></strong> have this |
        <strong id="got" class="stat">

    
      <%= @miniature.collections.where(status: 'Want').count(:user_id, distinct: true) %></strong> want this
