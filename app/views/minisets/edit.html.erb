<% provide(:title, 'Edit set') %>
<h1>Edit miniature set listing</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@miniset) do |f| %>
                <%= render 'fields', f: f %>
      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>


