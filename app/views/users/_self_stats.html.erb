<% @user ||= current_user %>


    You are following<a href="<%= following_user_path(@user) %>"><strong id="following" class="stat">
      <%= @user.followed_users.count %>
    </strong> <%= simple_pluralize(@user.followed_users.count, 'user') %>.
  </a><br />
 
    You have <a href="<%= followers_user_path(@user) %>"><strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong> <%= simple_pluralize(@user.followers.count, 'follower') %>.
  </a><br /><br />

  You have<strong id="collection" class="stat">
      <%= @user.collections.where(status: 'Got').count %>
    </strong> <%= simple_pluralize(@user.collections.where(status: 'Got').count, 'miniature') %> in <a href="<%= user_collections_path(@user, :status => "Got") %>">your collection</a>.
    Of which <strong id="collection" class="stat"><%= @user.collections.where(progress: 'Painted').count %></strong> are painted.<br /><br />

   You have <strong id="collection" class="stat">
      <%= @user.collections.where(status: 'Want').count %>
    </strong> <%= simple_pluralize(@user.collections.where(status: 'Want').count, 'miniature') %> in your <a href="<%= user_collections_path(@user, :status => "Want") %>">wantlist</a>.<br /><br />

   Your painting score is <strong id="collection" class="stat"><%= @user.vote_count %></strong>.
   <br /><br />

   You have cast <strong id="collection" class="stat"><%= @user.voted_count %></strong> votes for best painted miniature.




